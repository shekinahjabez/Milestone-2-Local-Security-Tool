# NetworkTraffic Analyzer - Dependencies
# Tkinter is bundled with Python's standard library.

# Runtime dependency
scapy>=2.5.0

# Build dependency (only needed to compile the portable binary)
pyinstaller>=6.0.0
